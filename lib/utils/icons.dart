import 'package:flutter/material.dart';

class CustomIcons{
  static const Icon vectorLeft =  Icon(Icons.chevron_left, size: 30, color: Colors.white);
  static const Icon swapArrow = Icon(Icons.swap_vert, color: Colors.white);
}